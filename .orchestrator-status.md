DONE — dira.sire.run website built per docs/plan/website.md, all 8 tasks;
rebased onto origin/main, dec-0030 id collision resolved, CI green.

Branch: site. PR: https://github.com/kazi-org/dira/pull/17 (not merged).
HEAD: adc844d.

CI: all 16 checks green (build+vet+test x2 platforms, build, gofmt+lint,
coverage+privacy+contrast, perf budgets x2, rendered contrast). Was red on
`TestWhyOnTheRealLedgerMatchesItsGoldenFile/dec-0012` before this fix.

Rebase fixup (this session): main's PR #13 merged its own dec-0030 (kazi
portfolio/status contract). This branch also minted dec-0030 (site
architecture) — an id collision. Kept main's dec-0030 verbatim, renumbered
this branch's entry to dec-0031 (next free id, verified by listing
.dira/entries/ post-rebase): renamed the file, updated its `id:` field,
docs/coverage.md's two obligation rows (`impl:dec-0030`→`impl:dec-0031` for
the site rows only — main's own dec-0030 rows untouched), and a stale
dec-0030 comment reference in .github/workflows/site.yml. `dira reindex` +
`go test ./schema/...` confirm the ledger is consistent (45 entries, 92
edges). CI's actual red cause: dec-0031's `derives_from: dec-0012` edge adds
a new "derived by" row to dec-0012's why-chain, and
cmd/dira/testdata/why/dec-0012.golden predated that edge — regenerated via
`go test ./cmd/dira -run TestWhy -update`, diff is the expected 4-line
addition only.

Verified this session (all foreground): go build/vet/gofmt/golangci-lint
clean; go test ./... and go test -race ./... green repo-wide; go test
./schema/... (ledger schema) green; site/: npm ci && npm run build clean;
npm run check:coherence OK; npx playwright test 3/3 passed; scripts/coverage.py
96/96 obligations covered; scripts/privacy-lint.py 4/4 checks pass. Pushed
--force-with-lease; PR commented with the full breakdown.

Earlier session's verification (unchanged by this fix, still holds):
strapline verbatim in dist/index.html; dist/why/dec-0001/index.html has real
"Elixir/OTP" + why_not (fetched from `dira ui` on this worktree's own binary,
no second renderer). Red→green witnessed: W1-T4 (removed a verb from the
curated map → build failed naming it; restored → passed); W2-T6 (deleted the
why snapshot → link test failed on the 404; rebuilt → passed); W2-T7 (mutated
README's strapline → check failed; restored → passed; same for a renamed verb
in a README code fence).

For the integrator: W1-T2 cites docs/growth/drafts/ as landing-copy source
but that dir has no landing prose — used docs/design/landing/ instead (the
real E8-L2 page; explained in that commit). Pre-existing, unrelated:
`docs/growth/scripts/check-drafts.mjs` fails on clean HEAD (.claude-plugin vs
hooks/settings.example.json PreCompact mismatch) — not run by pre-commit, not
touched here.
