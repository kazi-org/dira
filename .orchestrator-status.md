DONE — README.md rebased and reconciled against origin/main; PR #15 updated
(force-pushed, not merged, per instructions).

Branch: docs-refresh · PR kazi-org/dira#15

What changed since the last status: rebased onto origin/main (picked up
E2-L7's `dira import` lane, merged as PR #12, plus fbebb0c/dacf261/df64353/
cc2085f). Rebuilt /tmp/dira from this worktree and re-ran `--help`: verb count
is now 14, not 13 — `import` is a real, registered command. Reconciled the
README's verb table (moved `import` out of the "not shipped" placeholder row
into its real slot, matching `--help`'s order: after `reindex`, before
`version`), the top status blurb (13→14 verbs, mentions ADR import), and the
closing Status section (13→14).

Added a new "## How import works" section with a real observed example: built
a scratch ledger in /tmp (never this repo's own .dira), seeded via `dira init
-interview`, then ran `dira import DIR` against two real ADRs copied from the
vendored `internal/importadr/testdata/corpora/bbc-tams` test corpus. Captured
the actual report-and-ask output (report mode is the default — `import DIR`
without `--yes` prints the report and asks before writing; nothing was
written until confirmed), then confirmed with `--yes` in the same scratch
ledger to capture one real generated `dec-*.md` entry's frontmatter
(`source.hook: import`, `source.excerpt` with the sha256) for the doc.
Grounded the "yields nothing → offers to index instead" claim and the
five-corpora evidence table in dec-0028, which is real and unchanged by the
rebase.

Verified: `go build ./...` and `go vet ./...` clean after rebase.
scripts/coverage.py PASS (92/92). scripts/privacy-lint.py PASS (4/4). git
status after run: only README.md changed — all /tmp scratch ledgers and
binaries removed, this repo's real .dira/ was never touched.

Red→green: N/A — docs task, no gate/test added.

For the integrator: docs/plan.md, docs/roadmap.md untouched, as instructed.
Only README.md was touched this pass.
