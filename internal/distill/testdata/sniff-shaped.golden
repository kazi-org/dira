1 of 3 · dec-0031 · staged
Stop · 09:00 · regex

decides   we are moving the queue reader behind the storage interface

excerpt   we are moving the queue reader behind the storage interface rather
          than taking a path

y stand behind it · n not a decision · e edit the because · u undo
q leave the rest
