---
id: qst-1002
kind: question
title: Which serialization format should the export command use?
state: open
created: "2026-08-01T11:00:00Z"
edges:
  - type: blocks
    to: dec-1002
    note: the export decision waits on a format survey nobody has run yet
---

Fixture only. This question is open, and its `blocks` edge targets `dec-1002` —
but that entry's own state is `superseded`, which must suppress the
`DecisionBlocked` row even though the blocking question is live. That is the case
this directory proves.
