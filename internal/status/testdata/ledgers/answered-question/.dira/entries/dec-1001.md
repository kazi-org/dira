---
id: dec-1001
kind: decision
title: Do not cache the widget renderer's output between runs
state: accepted
created: "2026-08-01T10:05:00Z"
alternatives:
  - option: Cache renderer output keyed by input hash
    why_not: >
      Measurement showed the render itself costs 4ms; a cache would add
      complexity to save less time than the cache lookup itself costs.
---

Fixture entry. `qst-1001`'s `blocks` edge targets this entry, and the question is
answered, so this entry must NOT appear in `DecisionBlocked` — the case this
directory proves.
