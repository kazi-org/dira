---
id: dec-1003
kind: decision
title: Store widget layout as a tree, not a flat list
state: accepted
created: "2026-08-01T12:00:00Z"
alternatives:
  - option: Store layout as a flat list with explicit parent references
    why_not: >
      Every rendering pass would need to reconstruct the tree from the list
      before it could walk it, which is the tree in a worse encoding.
---

Fixture only. Accepted, carries no `realized_by` edge, so this must appear in
`ToBePlanned`.
