---
id: dec-3001
kind: decision
title: Route the widget renderer through the new layout engine
state: accepted
created: "2026-08-14T12:00:00Z"
alternatives:
  - option: Keep the old renderer and patch it in place
    why_not: >
      The old renderer's layout pass is O(n^2) in widget count and the new
      engine is the whole reason this goal was proposed.
edges:
  - type: realized_by
    to: "kazi:prop-e45"
---

Fixture only, for E4-L3-T1. `prop-e45` appears in
`internal/kazi/testdata/kazi/portfolio-populated.json`'s `planned` array with
`goal_id: "e45"`, so this entry's `realized_by` edge must resolve to `"e45"`.
