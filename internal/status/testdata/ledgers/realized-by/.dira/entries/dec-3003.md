---
id: dec-3003
kind: decision
title: Retire the legacy import path once the new one lands
state: accepted
created: "2026-08-14T12:10:00Z"
alternatives:
  - option: Keep both import paths indefinitely
    why_not: >
      Two paths for the same job is the exact duplication this decision
      exists to remove.
edges:
  - type: realized_by
    to: "kazi:prop-does-not-exist-anywhere"
---

Fixture only, for E4-L3-T1. `prop-does-not-exist-anywhere` names no
`proposal_ref` in `portfolio-populated.json`'s `planned` array — the rejected-
or-unknown-upstream case (lane doc point 5). Must resolve to an `Unresolved`
row, never to `ToBePlanned`.
