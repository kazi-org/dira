---
id: dec-0008
kind: decision
title: dira integrates with kazi only through kazi's existing --json contract and hooks
state: accepted
created: "2026-07-29T20:00:00Z"
tags: [founding, kazi-seam, integration]
edges:
  - type: derives_from
    to: dec-0004
    note: derived status needs a stable read surface, not a private one
  - type: blocks
    to: qst-0005
    note: two of the three seams need small upstream additions
alternatives:
  - option: Read kazi's internal state directly — its database, run files, or proposal store
    why_not: >
      It would couple dira to kazi's internals, so every kazi refactor silently
      breaks dira, and it would make dira unusable against a kazi installed from
      Homebrew rather than a source checkout. The `--json` surface exists precisely
      to be depended on.
    revisit_if: never
  - option: Vendor kazi as a library and call it in-process
    why_not: >
      Impossible across the language boundary (dec-0001), and it would make kazi a
      hard dependency — breaking the requirement that dira degrade gracefully when
      kazi is absent (dec-0004).
  - option: Have kazi call dira instead, inverting the dependency
    why_not: >
      It would make the why-layer a required part of kazi's execution path, which is
      wrong on both sides: kazi must stay usable with no dira installed, and dira must
      not be able to break a convergence run.
source:
  hook: manual
  tier: human
confirmed_by: human
---

Verified against kazi at `docs/adr/`, `lib/kazi/cli.ex`, and `lib/kazi/cli/schema.ex`
on 2026-07-29. Three seams, in order of how much upstream work each needs.

**1. Status join — works today, undocumented.** `kazi portfolio --json` is
implemented (`cli.ex:1254`, `portfolio_json/1` at `cli.ex:4249`) and emits
`schema_version`, `kind: "portfolio"`, `planned`, `by_repo` (repo → bucket →
entries), `fleet_remote`, `totals` (`{base, empty, rows: [{bucket, count, pct}]}`),
`todo`, `blocked` (each carrying `cause` and a `blocker` label), and `rate`. Its
bucket enum is exactly `done | running | blocked | todo | planned`, which maps
cleanly onto dec-0004's join table.

The catch: `portfolio` is **not registered in `Kazi.CLI.Schema`**, which documents
only `apply`, `plan`, `bus`, and `status`. So the shape is versioned but not
discoverable, and nothing in kazi's test suite pins it as a public contract. dira
should depend on it *and* ask upstream to register it — that is issue 1 of 3.

**2. Rationale linkage — works today.** `kazi plan --adr --json` exists
(`cli.ex:620`, ADR-0019) and mints the proposal ref that `approve --json` flips
(ADR-0049). Proposal refs look like `prop-url-shortener-3f9c1a2b` and are stable, so
`realized_by: kazi:prop-…` is a durable edge. Goal-files are TOML.

**3. Disposition capture — needs upstream.** `kazi approve <ref>` and
`kazi reject <ref>` are the single front door for accept/reject decisions, and
subscribing to them is the highest-leverage capture point in the whole design: every
approval is already a decision event, so logging it costs the user zero new
ceremony. But kazi emits no post-disposition hook today. Until it does, dira's skill
wraps the commands instead — correct behaviour, worse ergonomics. That is issue 2.

Issue 3 is the optional `why = "dira:dec-NNNN"` field on goal-files, closing the
loop downward so a predicate wall can be traced back to the intent it serves.

The boundary itself is quoted from kazi's own README — it will never "decide what to
build — that's your judgment." That sentence is dira's charter: kazi is everything
downstream of a declared goal; dira is everything upstream of declaration.
