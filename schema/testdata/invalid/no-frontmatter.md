# Not an entry

This file lives in `.dira/entries/`-shaped storage but has no YAML
frontmatter, so there is nothing to validate. A reader that treats it as an
empty entry, or silently skips it, loses the file. It must be an error.
