# L2-E4-L1 — Build the kazi read surface

You are an L2 planner. Decompose **one lane** into tasks and write them to
`docs/plan/tasks/E4-L1.md`. Planning only — write no Go code.

## Context

**Epic:** E4 — Derived status (`docs/plan.md` §E4, roadmap M4). dira never stores
execution status; it joins its own ledger against kazi at read time (`dec-0004`), and
the only sanctioned way in is kazi's `--json` surface (`dec-0008`).

**Lane:** `E4-L1` — Build the kazi read surface: a typed client over
`kazi portfolio --json` and `kazi status <ref> --json`, pinned to recorded fixtures.
This lane touches kazi and **never** the ledger. It produces a package other lanes
consume; it renders nothing and buckets nothing.

**`acc:` (verbatim):**

> `go test ./internal/kazi` passes against `testdata/kazi/` holding ≥4 recorded fixtures
> (a populated multi-goal `portfolio --json`, an empty one, one hand-extended to carry
> all four `cause` values `dag|over_budget|error|stuck`, and one with non-empty
> `fleet_remote`), asserting: `rate.empty?` and `totals.empty` both decode to a non-zero
> field (the JSON keys genuinely differ); `by_repo` values decode under
> `in_progress|stuck|complete` while `totals.rows[].bucket` decodes under
> `done|running|blocked|todo|planned`, and a fixture that swaps the two enums **fails**
> to decode rather than decoding as a zero value; `Snapshot()` with no `kazi` on `PATH`,
> with a stub exiting 2, with a stub printing non-JSON, with a stub printing JSON whose
> `kind != "portfolio"`, and with a stub that sleeps past the deadline each returns
> `Unavailable` carrying a distinct machine-readable reason and a nil snapshot — and in
> no case a value a caller could mistake for an empty portfolio; a `schema_version`
> other than the pinned `2` yields a snapshot flagged `ContractDrift` naming the
> observed version, not an error; and `Status(ref)` decodes both `kind: "run"` and
> `kind: "proposal"` fixtures and reports a ref kazi does not know as `NotFound`
> distinct from `Unavailable`.

**depends_on:** `E0` (a buildable Go module and command skeleton). No dira lane.

## Read first

1. `docs/plan.md` §E4 — the L0 `acc:` line this lane serves part of.
2. `docs/plan/lanes/E4.md` — **especially the section "What I verified myself, and
   where the prompt and `dec-0008` are wrong."** Points 1–10 are the shape of the
   surface you are wrapping, each already checked against kazi source and a live run.
   Do not re-derive them; do not trust a summary that contradicts them.
3. `.dira/entries/dec-0008.md` — the seam decision. Note its §1 is partly wrong (see
   point 1); your first task corrects it rather than diverging silently.
4. `.dira/entries/dec-0004.md` — why this client may never write anything.
5. `.dira/entries/cst-0004.md`, `int-0002.md` — no network, no daemon. This client
   shells out to a local binary and does nothing else. **Do not use `kazi bus`**
   (`qst-0005` explains why: it needs `kazi daemon` and no-ops silently when it is down).
6. `docs/design.md` §6.2 — the surface as documented, which your first task corrects.
7. kazi source, read-only, if you want to confirm anything yourself:
   `/Users/dndungu/Code/kazi-org/kazi/lib/kazi/portfolio.ex` (bucket enums at lines 38
   and 55, `local_by_repo` and `run_entry` near 416–425, `planned_entries` near 400),
   `lib/kazi/cli.ex` (`portfolio_json/1` at 4249, `@run_schema_version` at 95),
   `lib/kazi/cli/schema.ex` (registered commands at 427; the `status` contract at 345).

No dira Go source exists yet. This lane creates `internal/kazi/`.

## Tasks — at most 8

Each task needs `id` (`E4-L1-T<n>`), `title`, `files` (real paths it will touch),
`acc:` (a command and an observable outcome, at task granularity), and `depends_on`.

Suggested spine — refine it, but do not lose a clause of the lane `acc:`:

1. **Record the contract findings and correct the record.** A new `.dira/` decision or
   note entry stating the two-enum split, the un-deduped `by_repo`, and the
   `kazi status` hybrid; plus the correction to `docs/design.md` §6.2 and a `note` on
   `dec-0008`. `acc:` must include that the new entry validates against
   `schema/entry.schema.json` and that no existing accepted entry was edited in place
   beyond adding an edge (`dec-0002` — one file per entry).
2. **Record the fixture corpus.** `testdata/kazi/portfolio-*.json` and
   `testdata/kazi/status-*.json`, captured with `kazi portfolio --json` and
   `kazi status <ref> --json`, plus the hand-extended `cause: dag` and `fleet_remote`
   variants (neither occurs naturally — point 9). The recording must include the
   multi-run `warnings-clean` case E4-L3 needs. `acc:` should assert the corpus covers
   every documented `cause` value and both `status` `kind`s, checked by a test over the
   fixture directory rather than by eye.
3. **The types.** Two distinct Go enums, not one. `json:"empty?"` on rate.
   `acc:` is the "swapped enums must fail to decode" clause.
4. **`Snapshot()` and typed unavailability.** Reason as an enumerated value, nil
   snapshot, bounded deadline, `kind` validation, exit code *not* trusted alone
   (point 10).
5. **The version gate.** `ContractDrift` naming the observed version; not an error
   (point 8). The CI-side contract test that fails on drift belongs here too.
6. **`Status(ref)`.** Both `kind`s, `NotFound` distinct from `Unavailable`, and the
   per-call cost recorded so E4-L3 can bound its fan-out (measured ≈0.65s).
7. **The stub-binary test harness.** Fake `kazi` executables on a temp `PATH` covering
   absent / exit 2 / garbage / foreign `kind` / hangs. Each must produce a *distinct*
   reason, asserted distinctly.

## Where to write

`docs/plan/tasks/E4-L1.md`.

## Stop rule

> Do not decompose further. L2 is the leaf level. If a task still feels too large to
> converge in one kazi run, say so explicitly in your output and propose the split — do
> not silently create an L3.

## Honesty rule

> If this lane cannot be planned because a prerequisite is unresolved, write that as your
> output instead of inventing tasks. A plan built on an unresolved question is worse than
> an admission that the question blocks it.
