# L2-E9-L1 — Register `portfolio` in `Kazi.CLI.Schema` (upstream ask 1 of 2)

You are decomposing one lane into tasks. **Do not write code and do not file the issue
yourself** — you produce the task plan; a later run executes it.

## Context

**Epic:** E9 — Upstream kazi. Two additive, non-breaking asks at `kazi-org/kazi`.
Independent of every other epic; can run at any time.
**Lane:** E9-L1 — file the ask that registers the `portfolio` command's `--json` result
shape in kazi's schema registry.
**depends_on:** none.

**Lane `acc:` line, verbatim:**

> an **open** issue exists at `kazi-org/kazi` whose body, read via
> `gh issue view <n> --repo kazi-org/kazi --json body,state`, contains all four of:
> (1) a reproduction of both halves of the gap as commands with their real observed output
> — `kazi schema portfolio` failing with `no result schema`, and `kazi portfolio --json`
> exiting 0 emitting `"kind":"portfolio"` with `"schema_version":2`;
> (2) a proposed `@schemas` entry enumerating every key `portfolio_json/1` actually emits,
> with no key absent and none invented;
> (3) a named kazi commit sha, and a `path:line` reference for every factual claim, each of
> which still quotes the content at that commit when re-checked;
> (4) no home path, personal username, or private hostname.
> And `gh issue list --repo kazi-org/kazi --state all --search "portfolio schema"` returns
> no *other* issue proposing the same registration.

## What is already verified — do not re-derive it, but do re-check line numbers

Verified read-only against `/Users/dndungu/Code/kazi-org/kazi` on 2026-07-29. kazi is a
separate repository; treat it as **read-only** for this lane.

- `portfolio_json/1` at `lib/kazi/cli.ex:4249` emits `schema_version`
  (`@run_schema_version`), `kind: "portfolio"`, `planned`, `by_repo`, `fleet_remote`,
  `totals`, `todo`, `blocked`, `rate`. Helpers: `portfolio_totals_json/1` at `:4269`
  (`{base, empty, rows: [{bucket, count, pct}]}`), `portfolio_blocked_json/1` at `:4277`
  (stringifies `cause`, adds a `blocker` label).
- Command parse at `lib/kazi/cli.ex:1252`; help descriptor at `cli.ex:417`; flags are
  `--full` and `--json` only. Bucket enum `done | running | blocked | todo | planned` at
  `cli.ex:4283`.
- `lib/kazi/cli/schema.ex` contains **zero** occurrences of `portfolio`. `@schemas` keys
  are exactly `"apply"` (`:36`), `"plan"` (`:176`), `"bus"` (`:252`), `"status"` (`:343`);
  `@ordered_commands` at `:419` is `["apply", "plan", "status", "bus"]`; `fetch/1` at
  `:447` reads `@schemas`, so `kazi schema portfolio` does not resolve.

**Two corrections you must respect.**

1. `dec-0008` and `docs/roadmap.md` both claim "nothing in kazi's test suite pins it as a
   public contract." **That is false.** `test/kazi/cli_portfolio_test.exs:181` — the test
   "v1 --json keys are byte-identical and new keys are additive" — decodes
   `portfolio --json` and asserts `schema_version == 2`, `kind == "portfolio"`, and the
   shape of every v1 and additive key. Correcting the dira-side entries is T1; the issue
   must never repeat the false version.
2. The true, stronger ask: the shape is already stable **and** already test-pinned, so
   registration is nearly free. What is missing is **discoverability** (an agent cannot
   `kazi schema portfolio` to introspect a shape kazi does emit) and **governance** (the
   moduledoc establishes a lockstep-version rule across the registered schemas, and
   `portfolio` sits outside it). A supporting second data point, verified:
   `docs/schemas/roadmap.md:8` claims `kazi schema roadmap` emits a descriptor, but
   `roadmap` appears nowhere in `schema.ex` — the registry has drifted from
   `docs/schemas/` in two different ways.

**Framing rule.** Lead with kazi's own interest: an emitted-but-undocumented `--json`
shape is a gap in kazi's public contract whether or not anything consumes it. dira is a
motivating consumer mentioned once, not the argument.

## Read first

- `docs/plan.md` §E9 — the epic and its `acc:` line.
- `docs/plan/lanes/E9.md` — this lane, the other lane, and the full verification record.
- `.dira/entries/qst-0005.md` — the two asks, the reduction from three, and the fallbacks.
- `.dira/entries/dec-0008.md` — seam 1 is the paragraph T1 amends.
- `docs/roadmap.md` §"Upstream asks" — the row T1 amends and T6 fills in.
- In kazi, read-only: `lib/kazi/cli/schema.ex`, `lib/kazi/cli.ex` around `:1252`, `:4234`,
  `:4249`, `:4269`, `:4277`, and `test/kazi/cli_portfolio_test.exs:181`.

## Constraints that bind this lane

- **Scrub internal details.** No home paths, no personal usernames, no private hostnames
  in the issue body or in any committed draft. Cite kazi paths repo-relative.
- **kazi is read-only here.** This lane files an ask; it does not implement the change or
  open a PR against kazi.
- `dec-0008`: dira couples to kazi only through `--json` and hooks — never internals. That
  is *why* the emitted shape needs to be a contract.
- The issue must not duplicate an existing one. Verified today: 8 open issues at
  `kazi-org/kazi`, none relevant; `--state all` searches for "portfolio schema",
  "Kazi.CLI.Schema" surface only closed, unrelated issues. Re-check at file time anyway.

## The task bound — at most 8 tasks

Suggested shape; you own the final decomposition. Each task needs `id`, `title`, `files`,
`acc:`, `depends_on`.

- `E9-L1-T1` — correct the false test-suite claim in `dec-0008` and `docs/roadmap.md`.
  Files: `.dira/entries/dec-0008.md`, `docs/roadmap.md`.
- `E9-L1-T2` — re-verify every cited `path:line` at a pinned kazi commit sha and record
  the sha plus the quoted content.
- `E9-L1-T3` — capture the reproduction: run both commands, record real output verbatim.
- `E9-L1-T4` — draft the proposed `@schemas` entry field-by-field from `portfolio_json/1`,
  `portfolio_totals_json/1`, `portfolio_blocked_json/1`.
- `E9-L1-T5` — assemble the issue body as a committed draft, then duplicate-check and
  file. Suggested draft path: `docs/upstream/kazi-issue-1-portfolio-schema.md`.
- `E9-L1-T6` — record the filed issue number in `qst-0005` and the roadmap's Upstream-asks
  table.

Every `acc:` must be objectively checkable by a command, at task granularity. `acc: the
issue is well written` is not a predicate. Neither is `acc: a draft file exists` — that
goes green the moment an empty file is touched. Check each one against the current repo
state and say so if it is already green.

## Where to write

`docs/plan/tasks/E9-L1.md`.

## The stop rule

> Do not decompose further. L2 is the leaf level. If a task still feels too large
> to converge in one kazi run, say so explicitly in your output and propose the
> split — do not silently create an L3.

## The honesty rule

> If this lane cannot be planned because a prerequisite is unresolved, write that
> as your output instead of inventing tasks. A plan built on an unresolved question
> is worse than an admission that the question blocks it.
