# L1-E5 — Tiers — BLOCKED on qst-0001

**Read `docs/plan/prompts/_L1-CONTRACT.md` first.** It defines lane structure, the
`acc:` rules, the bounds, and how to emit your L2 prompts. This file supplies scope only.

**Your epic:** E5 — Tiers — BLOCKED on qst-0001
**L0 `acc:` line:** see the E5 section of `docs/plan.md`, verbatim. Your lanes must
collectively satisfy it; no single lane needs to.

## This epic is blocked. Plan to lane level ONLY.

**Do not emit L2 task prompts.** `qst-0001` is unresolved: when a public repo ledger
carries `derives_from: sire:int-0002` and `sire` is private, a stranger cloning the
repo sees a dangling pointer into a ledger they cannot read. Three candidate answers
are recorded in the entry and none is obviously right.

Tasks written against an unresolved privacy model would be rewritten wholesale, and
`cst-0003` makes getting this wrong a **security** problem rather than a rework
problem — a private strategic note committed into public git history cannot be
un-published.

**Your first lane must be "answer qst-0001".** It is a decision lane, not a build
lane: its `acc:` is that the entry reaches `state: answered` with a recorded rationale
and the rejected candidates preserved. Note in your output that the remaining lanes
are provisional pending that answer.

## Scope (provisional)

Workspace and personal ledgers, namespaced ref resolution through `[parents]` in
config, the orphan-work drift flag, `dira init --interview`.

## Specific things to get right

- **`init --interview` is the hero experience, not setup.** `dec-0010` promoted it:
  `dira init` may **never** produce an empty ledger, because a fresh ledger that says
  nothing is the fatal first impression.
- The few-intents cap matters most at the personal tier — twelve active intents *is*
  the absence of clarity, made visible. Do not design that away.
- Inheritance is one-way and read-time only (`cst-0003`).

## Read also

`qst-0001`, `dec-0006`, `cst-0003`, `dec-0010`.
