# L2-E9-L2 — Post-disposition hook on `approve`/`reject` (upstream ask 2 of 2)

You are decomposing one lane into tasks. **Do not write code and do not file the issue
yourself** — you produce the task plan; a later run executes it.

## Context

**Epic:** E9 — Upstream kazi. Two additive, non-breaking asks at `kazi-org/kazi`.
Independent of every other epic; can run at any time.
**Lane:** E9-L2 — file the ask for a post-disposition hook fired when a proposal is
approved or rejected. This is the highest-leverage capture point in dira's design: an
approval already *is* a decision event, so logging it costs the user zero new ceremony.
**depends_on:** none. (E9-L1 is the sibling ask; the two do not block each other.)

**Lane `acc:` line, verbatim:**

> an **open** issue exists at `kazi-org/kazi` whose body contains all five of:
> (1) the entry points named with `path:line` — `execute_approve/2` and `execute_reject/2`
> in `lib/kazi/cli.ex`, and `Kazi.Authoring.approve/2` / `reject/2` — each re-checked
> against a named commit;
> (2) a fenced proposed contract specifying the config key, the invocation (synchronous,
> before the command's own exit), the payload fields (proposal ref, new state, timestamp),
> and the failure policy (whether a non-zero hook fails the disposition);
> (3) the verbatim ADR-0067 sentence "degrades gracefully when the daemon is not running:
> bus surfaces report 'no daemon' cleanly and no-op" with its
> `docs/adr/0067-session-coordination-bus.md` line reference, given as the reason the ask
> is **not** `bus post`, plus the ~24h retention and ~1 KB cap;
> (4) an explicit fallback paragraph stating that dira's skill wraps the commands if this
> is declined, so the ask is not a blocker;
> (5) no home path, personal username, or private hostname.
> And `gh issue list --repo kazi-org/kazi --state all --search "approve reject hook"`
> returns no other issue proposing a disposition hook.

## What is already verified — do not re-derive it, but do re-check line numbers

Verified read-only against `/Users/dndungu/Code/kazi-org/kazi` on 2026-07-29. kazi is a
separate repository; treat it as **read-only** for this lane.

- Entry points: `execute_approve/2` at `lib/kazi/cli.ex:8130`, `execute_reject/2` at
  `lib/kazi/cli.ex:8229`, over `Kazi.Authoring.approve/2` at `lib/kazi/authoring.ex:474`
  and `reject/2` at `:537`. `approve <ref>` / `reject <ref>` are the single front door for
  accept/reject.
- **No post-disposition hook exists today.** Greps for `post_disposition`, `on_approve`,
  `after_approve`, and "disposition hook" across `lib/` and `docs/` return nothing.
- **The bus is disqualified, on kazi's own text.**
  `docs/adr/0067-session-coordination-bus.md:59-60`: "Everything else in this ADR degrades
  gracefully when the daemon is not running: bus surfaces report 'no daemon' cleanly and
  no-op." The `event`/`intent` stream is an "age-bounded, ~24h" limits-retention stream
  (`:76`) with "a hard per-message size cap (~1 KB)" (`:89`). So a disposition made with
  the daemon down would vanish with nothing indicating it had — disqualifying for a
  capture path — and the shape is wrong for a durable record besides.
- Hook-installation precedent worth citing as the model rather than inventing one:
  `kazi install-hooks` at `lib/kazi/cli.ex:440` (ADR-0071/ADR-0076) — merge-never-clobber,
  idempotent, an operator's own keys survive byte-identically, `--uninstall` removes
  exactly what it added.

**Framing rule — this is the ask that may be refused, and refusal would be reasonable.**
kazi's README stakes out a boundary: it will never decide what to build. A hook on the
command where a human records a judgement sits close to that line, and `qst-0005` records
the risk that the ask reads as dira colonising kazi's surface. So:

- Lead with the generic case: a disposition is a state transition any downstream tool
  might want to observe, and kazi already emits nothing on it. Mention dira once, as a
  motivating consumer.
- State the `qst-0005` fallback in the body — the skill wraps `approve`/`reject`, or dira
  diffs proposal state between sessions: worse ergonomics, same outcome. The ask must not
  read as a blocker on kazi.
- Pre-empt the obvious objection ("ride the existing bus rather than add
  infrastructure") with the ADR-0067 quote. An earlier draft of `qst-0005` proposed
  exactly that and was wrong on verification; say why in the issue.

## Read first

- `docs/plan.md` §E9 — the epic and its `acc:` line.
- `docs/plan/lanes/E9.md` — this lane, the sibling lane, and the full verification record.
- `.dira/entries/qst-0005.md` — the ask, the "explicitly not the session bus" reasoning,
  the honest refusal risk, and the fallback.
- `.dira/entries/dec-0008.md` — seam 3 is this ask; it is the only seam that genuinely
  needs kazi to change.
- `.dira/entries/cst-0004.md` and `.dira/entries/int-0002.md` — why a daemon dependency is
  out of bounds for dira's capture path.
- `docs/roadmap.md` §"Upstream asks" and §"Blocked" — the two rows a later task updates.
- In kazi, read-only: `docs/adr/0067-session-coordination-bus.md`, `lib/kazi/cli.ex`
  around `:440`, `:8130`, `:8229`, and `lib/kazi/authoring.ex:474`/`:537`.

## Constraints that bind this lane

- **Scrub internal details.** No home paths, no personal usernames, no private hostnames
  in the issue body or in any committed draft. Cite kazi paths repo-relative.
- **kazi is read-only here.** File the ask; do not implement the hook or open a PR against
  kazi. If the proposed contract needs a prototype to be credible, say so in your output
  rather than quietly planning one.
- The proposal must be a **direct synchronous** invocation and must not require
  `kazi daemon`. A hook that no-ops when a daemon is down fails the one purpose it has.
- `cst-0004` / `int-0002`: dira's capture path must work with the network unplugged and
  with no long-lived process. That is the constraint driving the shape of the ask.

## The task bound — at most 8 tasks

Suggested shape; you own the final decomposition. Each task needs `id`, `title`, `files`,
`acc:`, `depends_on`.

- `E9-L2-T1` — re-verify the entry points and the ADR-0067 quotes at a pinned kazi commit
  sha; record the sha and the quoted lines.
- `E9-L2-T2` — confirm no post-disposition hook has landed since the check above, and
  record the greps that establish it.
- `E9-L2-T3` — specify the proposed contract: config key, synchronous invocation point,
  payload fields, failure policy, and the `install-hooks` precedent it mirrors.
- `E9-L2-T4` — write the "why not the bus" section from the ADR-0067 quotes, and the
  fallback paragraph from `qst-0005`.
- `E9-L2-T5` — assemble the issue body as a committed draft, then duplicate-check and
  file. Suggested draft path: `docs/upstream/kazi-issue-2-disposition-hook.md`.
- `E9-L2-T6` — record the issue number in `qst-0005`, the roadmap's Upstream-asks table,
  and the roadmap's Blocked row for automatic disposition capture.

Every `acc:` must be objectively checkable by a command, at task granularity. `acc: the
contract is well specified` is not a predicate; `acc: a draft file exists` is vacuously
green the moment an empty file is touched. A greppable assertion over
`gh issue view <n> --json body` is. Check each predicate against the current repo state
and say so if it is already green.

**One thing to decide and state, not to leave implicit:** the failure policy. If a hook
exits non-zero, does the disposition still stand? Both answers are defensible and the
issue is weaker if it ducks the question. Pick one, say why, and note that kazi's
maintainer may choose the other.

## Where to write

`docs/plan/tasks/E9-L2.md`.

## The stop rule

> Do not decompose further. L2 is the leaf level. If a task still feels too large
> to converge in one kazi run, say so explicitly in your output and propose the
> split — do not silently create an L3.

## The honesty rule

> If this lane cannot be planned because a prerequisite is unresolved, write that
> as your output instead of inventing tasks. A plan built on an unresolved question
> is worse than an admission that the question blocks it.
