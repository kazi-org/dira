# L2-E4-L4 — `dira map`: the intent tree grouped by why

You are an L2 planner. Decompose **one lane** into tasks and write them to
`docs/plan/tasks/E4-L4.md`. Planning only — write no Go code.

## Context

**Epic:** E4 — Derived status (`docs/plan.md` §E4, roadmap M4).

**Lane:** `E4-L4` — Build `dira map`: the intent tree with derived roll-ups, plus a
`--json` shape E6 can render. `kazi portfolio` groups by **goal**; `dira map` groups by
**why**. `docs/design.md` §6.4 calls that *"the structural difference, not a cosmetic
one"* — it is the whole reason the command exists.

**`acc:` (verbatim):**

> `go test ./internal/cli -run TestMap` passes against golden files: `dira map` over the
> fixture ledger emits the intent tree grouped by `derives_from` parent in the shape
> `docs/design.md` §6.4 records, each intent's roll-up line's counts equalling the sum of
> its own children's buckets (a test that mutates one child's bucket and asserts the
> roll-up changes); all six `dec-0004` buckets appear across the golden files, each with a
> distinct label; an entry with no `derives_from` parent appears under an explicit
> unparented group, proved by a test asserting rendered-entry count equals ledger-entry
> count so nothing is dropped; `--json` emits a documented shape whose bucket names are
> dira's six, not kazi's five or three; and `dira map --json` is byte-identical across two
> consecutive runs on an unchanged ledger after deleting the single top-level
> `observed_at` field.

**depends_on:** `E4-L3` (the join producing six buckets).

## Read first

1. `docs/plan.md` §E4 — the L0 `acc:` line.
2. `docs/plan/lanes/E4.md` — the lane table, your risk (rebuilding `kazi portfolio` in
   dira's clothing), and **the boundaries section**, which settles three things you would
   otherwise have to decide:
   - **the drift row is E5's**, blocked on `qst-0001`. You emit the unparented group;
     you do not implement the orphan flag or tier-crossing refs;
   - **the web presentation is E6's.** `docs/design/screens/s2-index.html` is E6's
     target, not yours;
   - **you must expose all six buckets** even though that screen shows four labels.
     Collapsing six into four is E6's presentation decision made from a complete input.
3. `docs/design.md` §6.4 — the exact sample output, including the roll-up form
   `3 done · 1 running · 1 stuck` and the per-entry forms `→ converged ✓`,
   `→ to be planned (no goal yet)`, `⛔ blocks dec-0060`. Reproduce this shape; do not
   invent a second one.
4. `docs/design/screens/s2-index.html` — for **vocabulary only**: `converged`,
   `in motion`, `to be planned`, `blocks this`, and the per-intent roll-up
   (`2 converged · 1 in motion`). Note these are four labels over six buckets; where the
   CLI needs a label the screen does not supply (*Planned*, *Execution-blocked*), pick one
   and record the choice in your task file rather than folding the bucket away.
5. `.dira/entries/dec-0004.md` — the six buckets and their names.
6. `.agents/product-marketing.md` — binding on every line of copy this command prints.
7. `.dira/entries/cst-0001.md` — the 1,500-token brief cap. `dira map` is **not** the
   brief and must not become a second context injection; if a task starts adding
   `--context`, that belongs to E1's `dira brief`, not here.
8. `internal/status/` as E4-L2/E4-L3 leave it. You consume its result type; you do not
   re-derive buckets in the renderer.

## Where the boundaries are, so you do not build someone else's thing

- **`dira why` and the chain walker are E1's.** You group entries under their
  `derives_from` parent one level; you do not walk chains.
- **The inbound/parent lookup is E1's cache.** Consume it.
- **Filtering, sorting, and paging at scale**: `docs/design/DESIGN.md` records
  chain-at-scale collapse as an open design question owned by **E6**. If the tree is
  unreadable at 200 entries, say so in your output as a finding for E6 rather than
  inventing a collapse rule here.

## Tasks — at most 8

Each task needs `id` (`E4-L4-T<n>`), `title`, `files`, `acc:`, `depends_on`.

Suggested spine — refine it, but do not lose a clause of the lane `acc:`:

1. **The command skeleton and wiring** — `dira map` registered, exit codes defined,
   `--json` flag. `acc:` must be behavioural, not "the command exists" (a bare stub
   passing is the failure mode the plan bounds exist to prevent).
2. **Grouping by `derives_from`** — including the explicit unparented group and the
   count-conservation test (rendered entries == ledger entries).
3. **Per-intent roll-ups** — counts summed from children, with the mutation test.
4. **Labels for all six buckets** — distinct, and the two the design screen does not name
   chosen and recorded.
5. **The `--json` shape** — documented in the same commit, dira's six bucket names, plus
   the `observed_at` field and determinism after deleting it.
6. **The degradation line as rendered** — the unavailability statement's wording and
   placement. E4-L5 asserts its content; you decide its copy against
   `.agents/product-marketing.md`.
7. **Golden files** — covering all six buckets across at least: the real `.dira/` ledger,
   a fixture with an unparented entry, and a kazi-unavailable run.

## Where to write

`docs/plan/tasks/E4-L4.md`.

## Stop rule

> Do not decompose further. L2 is the leaf level. If a task still feels too large to
> converge in one kazi run, say so explicitly in your output and propose the split — do
> not silently create an L3.

## Honesty rule

> If this lane cannot be planned because a prerequisite is unresolved, write that as your
> output instead of inventing tasks. A plan built on an unresolved question is worse than
> an admission that the question blocks it.
