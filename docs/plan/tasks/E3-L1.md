# E3-L1 — tasks (as-built)

**Lane:** `docs/plan/lanes/E3.md` §E3-L1. **Prompt:** `docs/plan/prompts/L2-E3-L1.md`.
**Executed as data + prose only** — this lane's re-scope forbade any `.go`
file or `go.mod` change; every task below is a fixture, a corpus, a ledger
entry, or a written spec. Full report: `docs/decisions-pending/E3-L1-report.md`.

7 tasks (≤8 per the L2 bound).

---

### E3-L1-T1 — Author the conflict-detection decision entry

**Files:** `.dira/entries/dec-0014.md`

**acc:** `grep -rl "conflict detection" .dira/entries/` returns exactly one
path, `.dira/entries/dec-0014.md`; `go test ./schema -run
'TestLedgerValidates|TestTimestampsAreQuotedOnDisk'` passes (the file
validates against `schema/entry.schema.json` and its timestamps are
quoted); `dec-0014.md`'s `alternatives` array has length `>= 3`, every
`why_not` is non-empty, and at least one `why_not` contains the substring
`"dec-0003"`.

**depends_on:** none

---

### E3-L1-T2 — Build the `daemon` fixture ledger

**Files:** `internal/enforcer/testdata/ledgers/daemon/{int-0002,dec-0060,dec-0042,dec-0075,cst-0004,qst-0007,dec-0081,dec-0082,dec-0083}.md`

**acc:** all 9 files exist; each parses as YAML frontmatter + markdown body
and validates against `schema/entry.schema.json` — reproducible today via
the `python3` command in `docs/decisions-pending/E3-L1-report.md` (no Go
package exists yet at `internal/enforcer` to run this as `go test`); the 9
files jointly cover every row of the enforcement-set table in
`docs/plan/lanes/E3.md` at least once: `decision/accepted` (`dec-0060`,
`dec-0081`, `dec-0082`, `dec-0083`), `decision/rejected` (`dec-0042`),
`decision/staged` (`dec-0075`), `constraint/active` (`cst-0004`), `intent`
(`int-0002`), `question` (`qst-0007`) — checkable by `grep -h '^state:'
internal/enforcer/testdata/ledgers/daemon/*.md | sort -u` returning exactly
`{accepted, active, answered, rejected, staged}` and `grep -h '^kind:'
internal/enforcer/testdata/ledgers/daemon/*.md | sort -u` returning exactly
`{constraint, decision, intent, question}`.

**depends_on:** none

---

### E3-L1-T3 — Author the labeled corpus and freeze it by checksum

**Files:** `internal/enforcer/testdata/corpus.yaml`,
`internal/enforcer/testdata/corpus.sha256`

**acc:** `corpus.yaml` has `>= 40` rows (has 43); references `>= 8` distinct
entries under `ledgers/daemon/` (references 9); `>= 15` rows are labeled
`compliant` near-misses each sharing `>= 2` content words with the entry
named in its `near_miss_of` (has 19); every `expect: conflict` row's
`why_not_substring` is a literal substring of its cited entry's parsed
matchable text (title + alternatives + whitespace-normalized body) —
reproducible today via the `python3` verification script transcribed as
prose in `docs/decisions-pending/E3-L1-report.md`; `shasum -a 256
internal/enforcer/testdata/corpus.yaml` (first field) equals the trimmed
contents of `corpus.sha256`.

**depends_on:** E3-L1-T2

---

### E3-L1-T4 — Specify `TestCorpusWellFormed` in prose

**Files:** `docs/decisions-pending/E3-L1-report.md`

**acc:** the report names the exact path (`internal/enforcer/corpus_test.go`),
package (`enforcer`), and function (`TestCorpusWellFormed`), and states
assertions covering freeze / row-count / row-shape / distinct-entries /
near-miss-count — checkable by `grep -c "TestCorpusWellFormed"
docs/decisions-pending/E3-L1-report.md` returning `>= 1` and each of
`grep -c "Freeze"`, `grep -c "Row count"`, `grep -c "Distinct entries"`,
`grep -c "Near-miss rows"` against the same file returning `>= 1`. This
lane writes no `.go` file — the spec exists only in prose, for a later
lane (most likely E3-L2) to implement.

**depends_on:** E3-L1-T3

---

### E3-L1-T5 — Propose the constraint-level `revisit_if` schema field, unapplied

**Files:** `.dira/entries/dec-0014.md` (§ "(a) Does a `constraint` gain an
entry-level `revisit_if`?")

**acc:** `dec-0014.md` contains the literal JSON property name
`"revisit_if"` inside a fenced code block under that section
(`grep -A3 '"revisit_if"' .dira/entries/dec-0014.md` shows a `type`/
`description` pair); `schema/entry.schema.json` is unmodified by this lane
— `git diff --stat schema/entry.schema.json` produces no output.

**depends_on:** E3-L1-T1

---

### E3-L1-T6 — Settle the frontmatter-vs-body read cost against `int-0002`

**Files:** `.dira/entries/dec-0014.md` (§ "(b) Does the check read entry
bodies...")

**acc:** that section states which kinds are matched frontmatter-only
(`decision`) and which require a body read (`constraint`), and ties the
bound explicitly to latency — `grep -c "int-0002" .dira/entries/dec-0014.md`
returns `>= 1` and `grep -c "felt-latency" .dira/entries/dec-0014.md`
returns `>= 1`.

**depends_on:** E3-L1-T1

---

### E3-L1-T7 — Report the design.md §7 vs README/marketing §6 demo-block divergence

**Files:** `docs/decisions-pending/E3-L1-report.md`

**acc:** the report names the exact divergence (an extra `cst-0004`
conflict block present in `docs/design.md` §7 and absent from `README.md`
and `.agents/product-marketing.md` §6) and states that the fixture/corpus
were built to match the frozen one-conflict version — checkable by
`grep -c "cst-0004" docs/decisions-pending/E3-L1-report.md` in the
divergence section returning `>= 1`, and by inspecting `row-001` in
`corpus.yaml`, which names only `dec-0060` as its expected conflict.
Resolving which doc is authoritative is left to whoever owns marketing/demo
sign-off — not decided by this lane.

**depends_on:** none

---

## Not decomposed further

No task here felt too large to converge in one kazi run once a Go module
exists to run it against; the stop rule does not apply. The one thing this
lane explicitly does **not** do, per its re-scope, is write or wire the
`TestCorpusWellFormed` Go test itself — that is left as T4's prose spec for
a later lane, not silently absorbed here.
