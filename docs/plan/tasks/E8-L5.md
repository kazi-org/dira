# E8-L5 — tasks (L2)

**Lane:** `docs/plan/lanes/E8.md` §E8-L5. **Status: executed**, not just decomposed —
per dispatch, this lane produced the real artifacts in the same pass that wrote this
task file (see `docs/decisions-pending/E8-L5-report.md`). All seven tasks below are
**done**; their `acc:` lines are green as of this commit, verified by running the
checker and its self-test, not by inspection.

**Bound:** 7 tasks (≤8). No task felt too large to converge in one kazi run — this
lane needed no Go build and no running binary, only markdown, JSON, and a
zero-dependency node script.

---

### E8-L5-T1 — Author the plugin manifest and its bundle-layout documentation

- **files:** `.claude-plugin/plugin.json`, `.claude-plugin/BUNDLE.md`
- **acc:** `.claude-plugin/plugin.json` parses as JSON; its `hooks` block's commands
  match `hooks/settings.example.json` event-for-event and string-for-string (proved by
  `checkHooksMatch` in T6/T7, not asserted by inspection); `BUNDLE.md` names the
  ownership boundary against E2 (`skills/dira/*` is E2's, this file's `hooks` is this
  lane's) and states the versioning contract (lockstep with the binary release,
  mirroring kazi's ADR-0077) without claiming a release exists.
- **depends_on:** none
- **status:** done. No `mcpServers` block, unlike kazi's plugin manifest — dira has no
  MCP server in scope (`dec-0008`). `version` is the literal placeholder
  `"0.0.0-unreleased"`, documented in `BUNDLE.md` as inert until `E0` ships a release
  pipeline that can pin it.

### E8-L5-T2 — Document the frontmatter contract, in one place

- **files:** `docs/growth/DRAFT-CONTRACT.md`
- **acc:** the contract (`status: awaiting-maintainer-approval`, `posted: false`, plus
  `target`/`kind`/`owner`) is documented exactly once, outside `docs/growth/drafts/`
  itself (so the checker's "every file under `drafts/`" rule has no carved-out
  exception for its own documentation), and is written so any future lane's draft
  (`E8-L6`'s Show HN post, Reddit post, X thread) can point at it instead of
  re-deriving the contract.
- **depends_on:** none
- **status:** done.

### E8-L5-T3 — Write the marketplace listing draft

- **files:** `docs/growth/drafts/marketplace-listing.md`
- **acc:** carries the T2 frontmatter contract; states plainly that no `dira` binary
  exists and the listing is not postable until it does; contains none of
  `.agents/product-marketing.md` §10's banned-hype lexicon; leads with dira, not kazi.
- **depends_on:** E8-L5-T2
- **status:** done.

### E8-L5-T4 — Write the awesome-list / hook-showcase submission drafts

- **files:** `docs/growth/drafts/awesome-list-prs.md`
- **acc:** carries the T2 frontmatter contract; names three real, currently-live
  targets (`hesreallyhim/awesome-claude-code`, `ianymu/awesome-claude-code-hooks`,
  `rdmgator12/awesome-claude-plugins`) with each target's submission method and
  required format **read from the live repository** (dated 2026-07-30), not assumed;
  states the gate (`E0` must ship) explicitly per target.
- **depends_on:** E8-L5-T2
- **status:** done. One target (`hesreallyhim/awesome-claude-code`) turned out to take
  an issue-form submission, not a PR — recorded as a correction inside the file rather
  than silently renamed to fit the deliverable's filename.

### E8-L5-T5 — Write the directory submissions draft

- **files:** `docs/growth/drafts/directory-submissions.md`
- **acc:** carries the T2 frontmatter contract; explicitly rules out Product Hunt with
  the `/distribution` reasoning (no live demo, no binary); explicitly marks Tier-4
  agent/MCP registries **not applicable** (dira has no MCP server) rather than
  omitting them silently; explicitly flags AlternativeTo as in tension with
  `.agents/product-marketing.md` §1's new-category bet rather than listing it
  reflexively.
- **depends_on:** E8-L5-T2
- **status:** done.

### E8-L5-T6 — Write the two-sided drafts/plugin checker

- **files:** `docs/growth/scripts/check-drafts.mjs`
- **acc:** `node docs/growth/scripts/check-drafts.mjs` exits 0 against the real,
  committed repo state (drafts contract + deny-list scan + hook-match), zero
  third-party dependencies (stdlib `node:fs`/`node:path`/`node:url` only).
- **depends_on:** E8-L5-T1, E8-L5-T3, E8-L5-T4, E8-L5-T5
- **status:** done, verified by direct run: `check-drafts: PASS`, exit 0.

### E8-L5-T7 — Author the bad fixtures and prove the negative path, then the positive one

- **files:** `docs/growth/scripts/fixtures/{bad-posted-true,bad-denylist,hook-mismatch}/**`,
  `docs/growth/scripts/check-drafts.selftest.mjs`
- **acc:** `node docs/growth/scripts/check-drafts.selftest.mjs` exits 0, and its output
  shows every fixture asserted **red** (a distinct, named reason each) and every real
  repo path asserted **green** — not merely a checker that has never been observed to
  fail.
- **depends_on:** E8-L5-T6
- **status:** done, verified by direct run. All three red cases fired on the first
  attempt (`posted: true`; `gh pr create`/`gh issue create`/`praw`/`tweepy`/social-POST;
  a `Stop` hook command renamed only in the fixture `plugin.json`), then all real-repo
  cases passed green. One real bug found and fixed during this: the deny-list scan
  initially flagged its own source file and its own documentation, because both
  necessarily quote the deny-list's trigger phrases (in a comment explaining the rule,
  and in assertion labels) — fixed with a three-file, named exclusion
  (`SELF_EXCLUDED_FILES`) rather than a fragile string-splitting trick that was tried
  first and failed the same run.
