# E8-L1 — tasks (L2)

**Lane:** `docs/plan/lanes/E8.md` §E8-L1. **Status: executed**, not just decomposed —
per dispatch, this lane produced the real artifacts in the same pass that wrote this
task file (see `docs/decisions-pending/E8-L1-report.md`). All five tasks below are
**done**; their `acc:` lines are green as of this commit, verified by running the
checker, not by inspection.

**Bound:** 5 tasks (≤8). No task felt too large to converge in one kazi run — none of
this required a Go build or a running binary, only markdown and a zero-dependency node
script, so the lane did not need to stop short of execution.

---

### E8-L1-T1 — Author the Bullseye channel pass

- **files:** `docs/growth/channels.md`
- **acc:** the file exists, contains exactly 19 rated channels and exactly 3 marked
  `inner`, and contains an explicit reconciliation section that states where this pass
  agrees and disagrees with product-marketing §7's five-channel ranking and why.
- **depends_on:** none
- **status:** done. Reconciliation disagrees with §7 on the third inner-ring slot —
  Content Marketing (build-in-public) was picked over the SEO/ledger-renderer channel
  §7 ranked, because the SEO mechanism depends on `dira render` (E6), which does not
  exist yet, and Content Marketing needs no unshipped infrastructure. See the file's
  "Reconciliation with product-marketing §7" section for the full reasoning and ICE
  comparison (210 vs. 180).

### E8-L1-T2 — Author the ICE backlog and the three pre-registered experiment specs

- **files:** `docs/growth/experiments.md`
- **acc:** the file's ICE backlog rates all 19 channels from T1, and carries exactly one
  `### EXP-NNN:` pre-registered spec per inner-ring channel, each with a threshold that
  is a rate (has a denominator) with an explicit `n≥` minimum and a deadline relative to
  an event, never an absolute date.
- **depends_on:** E8-L1-T1
- **status:** done. Three specs (EXP-001 Existing Platforms, EXP-002 Community Building
  / Show HN, EXP-003 Content Marketing), each with hypothesis, terminal metric,
  threshold, effort cap, on-pass scale rule, on-fail rule, owner, and cadence. All three
  terminal metrics use GitHub's own repo-owner Traffic Insights, not tool telemetry
  (`cst-0004` compliance note in `channels.md`).

### E8-L1-T3 — Seed the corpus schema

- **files:** `docs/growth/corpus.md`
- **acc:** the file exists with its column schema and an empty results table — no data
  rows — and states explicitly why it is empty (no experiment has run; no binary
  exists).
- **depends_on:** none
- **status:** done.

### E8-L1-T4 — Write the two-sided growth-plan checker

- **files:** `docs/growth/scripts/check-growth-plan.mjs`
- **acc:** `node docs/growth/scripts/check-growth-plan.mjs` exits 0 against the committed
  `docs/growth/{channels,experiments}.md`, printing the channel/inner-ring/spec counts;
  zero third-party dependencies (stdlib `node:fs`/`node:path`/`node:url` only).
- **depends_on:** E8-L1-T1, E8-L1-T2
- **status:** done, verified by direct run: `growth plan OK: 19 channels rated, 3
  inner-ring, 3 pre-registered spec(s), 0 banned-hype terms outside honest-limits
  blocks.` exit 0.

### E8-L1-T5 — Author the three bad fixtures and prove the negative path

- **files:** `docs/growth/fixtures/{bad-missing-owner-cadence,bad-raw-count,bad-hype-term}/{channels,experiments}.md`, `docs/growth/fixtures/README.md`
- **acc:** `node docs/growth/scripts/check-growth-plan.mjs docs/growth/fixtures/<name>`
  exits non-zero with a distinct, named reason for each of the three fixtures.
- **depends_on:** E8-L1-T4
- **status:** done, verified by direct run against all three:
  - `bad-missing-owner-cadence` → `FAIL: channels.md inner-ring row "Existing
    Platforms" is missing an owner`
  - `bad-raw-count` → `FAIL: EXP-002's threshold is a raw count — missing a denominator
    (rate/percentage) and an n-minimum: "≥50 upvotes, within 7 days of the post."`
  - `bad-hype-term` → `FAIL: banned-hype term "revolutionary" found in channels.md
    outside any <!-- honest-limits:start/end --> block`
