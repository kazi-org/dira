# E8-L6 — tasks (L2) — the launch cannot run without a binary, and the checker must say so

**Lane:** `docs/plan/lanes/E8.md` §E8-L6. **Prompt:** `docs/plan/prompts/L2-E8-L6.md`.
**Status: decomposed, not executed.** `docs/growth/scripts/check-launch-readiness.mjs`
does not exist. Once it does, running it against the current repo state must exit
non-zero, naming the absent `dira` binary as the blocking gate — that is this lane's
own `acc:` line, and it is true by construction until E0–E3 ship.

**T0 = the day a stranger can install `dira` and have it work.** Every offset in
`docs/growth/launch.md` is relative to T0 (`T0`, `T0+7`, `T0+14`); no absolute date
appears anywhere in that file, and the checker fails on one.

**Correction to the L2 prompt, stated rather than silently applied.** The prompt names
`qst-0003` as still open and instructs "read this before sequencing anything." **It is
answered**, by `dec-0028` (2026-08-09, five days before this pass): semantic import,
gated by a mandatory pre-import dry run, because yield ranges 0%–90% across five real
corpora. This does not remove the launch risk — it sharpens it. The risk is no longer
"we don't know if import works," it is "the day-1 value claim is conditional on the
target repo's own corpus quality, and the launch copy must say that conditionally, not
promise it." `E8-L6-T2`'s known-limitations section states this precisely rather than
citing the stale open-question framing.

**7 tasks (≤8 per the L2 bound).** Waves: `[T1, T2, T3, T4, T5]` (each independently
authorable, no file overlap) → `[T6]` (the roadmap GTM section, needs the other five
lanes' status) → `[T7]` (the aggregation checker, composes everything above plus the
siblings it must not re-implement).

## Absolutes this lane is held to

1. **Nothing outward-facing is ever sent, posted, published, or submitted by an agent.**
   Every draft under `docs/growth/drafts/` carries the `E8-L5` frontmatter contract
   (`docs/growth/DRAFT-CONTRACT.md`) and validates under `check-drafts.mjs`. No task in
   this lane may post, schedule, DM, or email anyone, and no task's `acc:` line may
   require it to.
2. **No virality claims**, in the drafts or in `launch.md`'s internal targets.
3. **Every channel named in `launch.md` id-matches a pre-registered threshold row** in
   `docs/growth/experiments.md`. Orphans in either direction fail T7's checker.
4. **No launch without a binary.** T7's first gate is the binary's existence; until then
   it exits non-zero by design, and that is the correct, expected state today.

---

## Tasks

- [ ] E8-L6-T1 — Write `docs/growth/launch.md`, phase-sequenced, T0-relative only · verifies: [dec-0010, cst-0004] · deps: [none]
- [ ] E8-L6-T2 — Write `docs/growth/drafts/show-hn.md` · verifies: [dec-0010, dec-0003, dec-0028] · deps: [E8-L5-T2]
- [ ] E8-L6-T3 — Write `docs/growth/drafts/reddit-r-claudeai.md`, with its 9:1 contribution ledger · verifies: [dec-0010] · deps: [E8-L5-T2]
- [ ] E8-L6-T4 — Write `docs/growth/drafts/x-thread.md` · verifies: [dec-0010] · deps: [E8-L5-T2]
- [ ] E8-L6-T5 — Write the pre-send accuracy gate, two-sided against a vaporware fixture · verifies: [dec-0007, dec-0012] · deps: [none]
- [ ] E8-L6-T6 — Add the GTM section to `docs/roadmap.md` · verifies: [dec-0010] · deps: [none, informational: E8-L1..L5 status]
- [ ] E8-L6-T7 — Write `check-launch-readiness.mjs`, the ordered aggregation checker · verifies: [dec-0010, cst-0004, dec-0007] · deps: [T1, T2, T5, T6, E8-L5-T6, E8-L4-T1]

---

### E8-L6-T1 — Write `docs/growth/launch.md`, phase-sequenced, T0-relative only

Phase sequence per `/distribution`'s playbook and the sibling `kazi/docs/marketing/
launch-plan.md` precedent, adapted to dira's own channels from `docs/growth/channels.md`
and `docs/growth/experiments.md` (do not invent a new taxonomy): **Phase 0** — polish the
conversion surface (landing page and demo clip already gated by `E8-L2`/`E8-L4`, GitHub
social preview, repo topics); **Phase 1** — warm-audience presence (kazi's existing
audience, per product-marketing §7 item 2 — cross-linked docs, not an independent
inner-ring experiment per `channels.md`'s own reconciliation note); **Phase 2** — the one
sharp artifact, Show HN built on the demo clip (`EXP-002`); **Phase 3** — the cascade
(marketplace/awesome-list placement from `EXP-001`, directory submissions from `E8-L5`);
**Phase 4** — week+1 measurement against `docs/growth/experiments.md`'s pre-registered
thresholds, logged to `docs/growth/corpus.md`.

**Files:** `docs/growth/launch.md`

**acc:** every `- [ ]` step line matches a regex requiring both an owner token
(`@maintainer` or a named role) and a T0 offset (`T0`, `T0[+-]\d+`); zero lines in the
file match an absolute-date pattern (`\b\d{4}-\d{2}-\d{2}\b` or a month name followed by
a day number) — checked by a constructed fixture copy of `launch.md` with one absolute
date inserted into a step, which flips a grep-based two-sided check
(`docs/growth/scripts/check-launch-readiness.mjs`'s date-scan, built in T7) from pass to
a named failure quoting the offending line, proven before the real file is shown to
pass.

**depends_on:** none. **verifies:** `dec-0010` (T0 is a condition, never a date — the
whole reason this file exists in this shape), `cst-0004` (no step assumes a hosted
renderer is required to launch).

---

### E8-L6-T2 — Write `docs/growth/drafts/show-hn.md`

Title: neutral, specific, no superlatives, matching `^Show HN: dira – `. First comment,
in the candid-builder voice `/distribution`'s reference and product-marketing §10
prescribe: who built it, the pain in one sentence, architecture and tradeoffs
(`dec-0003`'s no-model-client boundary, `dec-0002`'s one-file-per-entry storage), and
**known limitations stated plainly** — this is the persuasion mechanism on HN, not a
liability to soften. The limitations section is where `dec-0028`'s conditional import
promise belongs: "capture quality depends on the agent driving it" (`dec-0003`), "import
quality depends on how your ADRs are written — some corpora yield nothing to enforce,
and dira says so before writing anything" (`dec-0028`), "no team tier yet" (`dec-0007`).

**Files:** `docs/growth/drafts/show-hn.md`

**acc:** carries the `E8-L5`/`DRAFT-CONTRACT.md` frontmatter (`status:
awaiting-maintainer-approval`, `posted: false`, `target: news.ycombinator.com`, `kind:
show-hn`, `owner: E8-L6`); the title line matches `^Show HN: dira – ` exactly; contains
zero terms from `check-growth-plan.mjs`'s `BANNED_HYPE_TERMS` list outside a marked
honest-limits block (reusing that exported list rather than a second copy, so the two
checkers cannot silently diverge on what counts as hype); `grep -c 'dec-0003\|dec-0028'
docs/growth/drafts/show-hn.md` is `>= 1` (the known-limitations section cites its own
evidence, not an assertion); `node docs/growth/scripts/check-drafts.mjs` passes with this
file present.

**depends_on:** `E8-L5-T2` (the frontmatter contract this draft carries).
**verifies:** `dec-0010`, `dec-0003`, `dec-0028`.

---

### E8-L6-T3 — Write `docs/growth/drafts/reddit-r-claudeai.md`, with its 9:1 contribution ledger

One earned subreddit, per the lane's own instruction ("one honest post in one earned
subreddit beats cross-posting to five") — r/ClaudeAI, because the ICP is definitionally
already there (`channels.md` row 19 / `EXP-002`'s audience). The subreddit's current
self-promo rule is recorded from the live subreddit sidebar/rules at the time this task
is executed, not assumed — a rule that has since changed is a finding for whoever
executes this, not something this planning pass can pin.

**Files:** `docs/growth/drafts/reddit-r-claudeai.md`

**acc:** carries the frontmatter contract (`target: reddit.com/r/ClaudeAI`, `kind:
reddit-post`); contains a markdown table under a `## 9:1 ledger` heading with exactly 10
rows — 9 genuine prior contributions (each with a non-empty description and a date) and
1 promotional post — `grep -A15 '## 9:1 ledger' docs/growth/drafts/reddit-r-claudeai.md
| grep -c '^|'` is `>= 11` (header + separator + 9 data rows, at minimum); states the
subreddit's self-promo rule as read at authoring time, with the read date recorded;
zero banned-hype terms outside an honest-limits block; `check-drafts.mjs` passes.

**depends_on:** `E8-L5-T2`. **verifies:** `dec-0010`.

---

### E8-L6-T4 — Write `docs/growth/drafts/x-thread.md`

Built on the demo clip, not thread-bait — the thread's first post links or embeds
`assets/demo/check.cast`'s social cut (`E8-L4-T6`) by path, and the thread is the clip
plus the same candid limitations, not a separate pitch.

**Files:** `docs/growth/drafts/x-thread.md`

**acc:** carries the frontmatter contract (`target: x.com` or `bsky.app`, `kind:
social-post`); references `assets/demo/check.gif` or `assets/demo/check.mp4` by path
(`grep -c 'assets/demo/check\.' docs/growth/drafts/x-thread.md` is `>= 1`); zero
banned-hype terms outside an honest-limits block; `check-drafts.mjs` passes.

**depends_on:** `E8-L5-T2`. **verifies:** `dec-0010`.

---

### E8-L6-T5 — Write the pre-send accuracy gate, two-sided against a vaporware fixture

The anti-vaporware step, run before the maintainer is asked to approve anything.
Mechanically: every `dira <verb>` mentioned in a draft under `docs/growth/drafts/` must
name a verb that actually appears in `README.md`'s own documented verb table — the same
source `check-coherence.mjs` (`E8-L2`) already treats as canonical, read from there
rather than a second hand-maintained allowlist.

**Files:** `docs/growth/scripts/check-launch-accuracy.mjs`,
`docs/growth/scripts/fixtures/vaporware-draft/claims-unshipped-verb.md`

**acc:** `node docs/growth/scripts/check-launch-accuracy.mjs` exits 0 against the real
`docs/growth/drafts/`, reporting the verb-claims it checked and their source (`README.md`
line range); pointed at the committed fixture `claims-unshipped-verb.md` (a draft
claiming `dira sync`, a verb absent from README's table), it exits non-zero naming the
file and the unverifiable verb — proven red before the real drafts are shown to pass.

**depends_on:** none. **verifies:** `dec-0007` (no promise of a hosted/paid capability
that doesn't exist), `dec-0012` (no claim of a serving mechanism dira does not have).

---

### E8-L6-T6 — Add the GTM section to `docs/roadmap.md`

One line per E8 lane, in the roadmap's existing Shipped / In progress / In flight /
Planned / Blocked shape — links to the plan rather than restating milestone status the
roadmap already owns elsewhere, per the lane's own instruction.

**Files:** `docs/roadmap.md` (GTM section only)

**acc:** `grep -c '^## GTM' docs/roadmap.md` is `1` (exactly one section, not a
duplicate); every line under it that names an E8 lane (`E8-L1` through `E8-L6`) also
contains an owner token and either a PR-number pattern (`#\d+`) or the literal string
`no PR yet` — checked with a fixture line missing both, which fails a
grep-based two-sided assertion before the real section is shown to pass.

**depends_on:** none structurally, but informationally reads the current status of
`E8-L1` (done), `E8-L2` (done), `E8-L3`–`E8-L6` (this pass, planned not executed) so the
section is accurate the day it lands rather than copied from an earlier state.
**verifies:** `dec-0010`.

---

### E8-L6-T7 — Write `check-launch-readiness.mjs`, the ordered aggregation checker

The point of this task, stated because plans of this shape usually get it wrong: **the
checker must be useful while red**, because it will be red for months. A single "not
ready" line is not a checklist; an ordered gate list with the first unmet gate named is.

**Files:** `docs/growth/scripts/check-launch-readiness.mjs`

**acc:** run today (no `dira` on `PATH`, no `assets/demo/check.cast` on disk), the script
exits non-zero and its **first** printed line names the binary as the blocking gate
(`FAIL: no dira binary found on PATH — E0–E3 have not shipped`) — this is the lane's own
`acc:` line's first clause, true by construction and asserted here rather than merely
claimed. Beyond that gate, in fixed order, it checks: `launch.md`'s T0-offset/no-absolute-date
rule (composing `T1`'s check rather than re-implementing the regex a second time — proven
by the script importing `T1`'s exported scan function, not duplicating its pattern);
every channel in `launch.md` id-matches a threshold row in `docs/growth/experiments.md`
with no orphan on either side (a fixture `launch.md` naming a channel absent from
`experiments.md` flips this gate to a named failure); `check-drafts.mjs` (`E8-L5-T6`)
passes, invoked as a subprocess or imported function, not reimplemented; `T5`'s accuracy
gate passes; `E8-L4-T1`'s duration probe passes against `assets/demo/check.cast` (the
gate is skipped with a named `"not yet recorded"` reason, not a false pass, while the
file is absent); `docs/roadmap.md` contains the `T6` GTM section with every item named;
and the HN title in `show-hn.md` matches `^Show HN: dira – ` with zero banned-hype
terms. **Two-sided at the aggregate level**: a fixture repo state with a stub `dira` on
`PATH` and every other gate pre-satisfied is constructed to flip the checker to exit 0,
printing every gate as passed in order — proving the checker is not structurally stuck
red regardless of what else is true, which is the property a checker like this most
needs and most often lacks.

**depends_on:** `T1`, `T2` (draft existence, via `check-drafts.mjs`), `T5`, `T6`,
`E8-L5-T6` (`check-drafts.mjs` itself), and `E8-L4-T1` (the duration probe, named so a
future edit does not quietly reimplement it here). **verifies:** `dec-0010`, `cst-0004`
(the checker itself needs no network — it reads only committed files), `dec-0007`.

---

## Not decomposed further

T1–T4 are each one markdown file with a mechanically checkable shape; none felt too
large to converge in one run. T5 is one script plus one fixture, mirroring `E8-L5`'s
own `check-drafts.mjs` shape closely enough that no split is warranted. T6 is a roadmap
section addition, prose plus a grep-checkable shape. T7 is the one task doing real
composition work, but it is explicitly forbidden from re-implementing its siblings'
logic — that constraint is what keeps it from growing into something that needs an L3
split; if a future editor finds themselves writing regex logic inside T7 that already
exists in T1 or T5, that is the signal the constraint was violated, not that the task
was mis-sized.

## What this lane does not own

The channel selection and thresholds (`E8-L1`, already shipped) — this lane's `launch.md`
must id-match them, not re-derive or re-rank them. The draft frontmatter contract
(`E8-L5-T2`) — every draft here carries it, none of them redefine it. The demo clip and
its probes (`E8-L4`) — `T7` composes `E8-L4-T1`'s duration probe rather than rebuilding
it, and `T4`'s X-thread references the clip by path rather than embedding a copy.

## Report

Task count: 7. Path: `docs/plan/tasks/E8-L6.md`. No `acc:` line is green today — the
lane's own top-level line requires the checker to exist and still report non-zero, which
is a state this pass has not built yet. **`qst-0003` no longer blocks the sequence** —
it is answered by `dec-0028`, and the correction above replaces the L2 prompt's
"unresolved" framing with the actual conditional risk (day-1 value depends on the target
repo's own corpus quality, stated in `show-hn.md`'s limitations section rather than
assumed away). Nothing in `.agents/product-marketing.md` looks wrong. One item refused
rather than invented: T3 names only one subreddit rather than a menu of candidates,
per the lane's own "one earned subreddit beats five" instruction — if the maintainer
wants a second, that is a new task, not a silent expansion of this one's scope.
