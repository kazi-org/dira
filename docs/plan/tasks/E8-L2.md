# E8-L2 — task breakdown

**Lane:** E8-L2 — build the landing page on the existing design system and gate it.
**Owner:** unassigned (executed directly this session; see
`docs/decisions-pending/E8-L2-report.md` for the full report).
**Stop rule observed:** 6 tasks, all leaf-level. None felt too large to converge in
one run; none needed an L3 split.

All 6 are done as of this writing — the predicates below were run and are green. They
are recorded in acc-line form (not past tense) so a future session can re-verify them
mechanically rather than trusting this file.

---

### E8-L2-T1 — Extract canonical marketing strings into one module

**files:** `docs/design/landing/canonical.mjs`
**depends_on:** none
**acc:** `node -e "import('./docs/design/landing/canonical.mjs').then(m=>{for(const k of ['HOOK','TAGLINE','NO_BINARY','INSTALL_LINE','CATEGORY']) if(!m[k]) throw new Error('missing '+k)})"`
exits 0. Red before the file existed (import fails); green with all five exports
present and non-empty.

### E8-L2-T2 — Write check-coherence.mjs gating README <-> product-marketing <-> page drift

**files:** `docs/design/scripts/check-coherence.mjs`
**depends_on:** T1
**acc:** `node docs/design/scripts/check-coherence.mjs` prints `COHERENCE PASS` against
the committed page, README.md, and `.agents/product-marketing.md`; editing any one
canonical string in `canonical.mjs` to a value absent from its declared source(s)
flips the same command to a named non-zero `COHERENCE FAIL`. Two-sided; verified live
by editing the tagline and reverting it.

### E8-L2-T3 — Build the landing page on tokens.css, reusing the s1/s2 idiom

**files:** `docs/design/landing/index.html`
**depends_on:** T1
**acc:** `grep -oE '#[0-9a-fA-F]{3,6}' docs/design/landing/index.html` returns only
`#0f151c` and `#f7f4ed`; the five canonical strings from `canonical.mjs` are present
in the rendered page (checked by T2's script, not re-implemented here).

### E8-L2-T4 — Extend render.mjs to enumerate landing/ 

**files:** `docs/design/scripts/render.mjs`
**depends_on:** T3
**acc:** `node docs/design/scripts/render.mjs r5 landing` prints `GATE PASS` (console
errors, failed requests, blank mount, byte-identical light/dark pair, layout shift —
zero of each) and writes 6 `r5-landing-*.png` files (3 viewports × 2 schemes) to
`docs/design/renders/`.

### E8-L2-T5 — Run contrast.mjs as an inherited gate

**files:** `docs/design/scripts/contrast.mjs` (read-only — not authored in this lane;
E6 already shipped it, see the STALE-SPEC CORRECTION at the top of
`docs/plan/prompts/L2-E8-L2.md`)
**depends_on:** none
**acc:** `node docs/design/scripts/contrast.mjs` prints `CONTRAST PASS` — 42 ink/surface
pairs across 2 schemes, 0 failures, hover exceeds rest on all six surface/scheme
combinations. Already green; this task is "run and treat as inherited," not "write."

### E8-L2-T6 — Verify the two dira-specific laws hold on the new page

**files:** `docs/design/landing/index.html` (verification only, no separate script —
folded into manual review since DESIGN.md's laws 1 and 3 are grep-checkable inline)
**depends_on:** T3
**acc:** `grep -n 'caught\|class="fail"' docs/design/landing/index.html` shows `--caught`
used only via the page's one `.fail` rule, applied only to the `dira check` failure
line (a genuine check-failure, not a rejected alternative — Law 1); `grep -ic
'<svg\|<canvas\|<img' docs/design/landing/index.html` returns `0` (Law 3 — the demo
transcript and the legend are real, selectable text, never a picture of glyphs).

---

## Deviation from the L2 prompt, recorded here rather than silently

`docs/plan/prompts/L2-E8-L2.md` is written as an L2 **planning** prompt ("Planning
only — write no code, no copy, no HTML"), decomposing E8-L2 into these same six
leaf tasks. The team lead's dispatch for this session asked for direct execution
(build the page and gate it, not just plan it), so this file documents the
decomposition **and** all six tasks were actually built and verified green in the
same pass, rather than being handed off. No task here was invented without first
being run.
