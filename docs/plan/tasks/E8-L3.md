# E8-L3 — tasks (L2)

**Lane:** `docs/plan/lanes/E8.md` §E8-L3. **Prompt:** `docs/plan/prompts/L2-E8-L3.md`.
**Status: decomposed, not executed.** Nothing below has been run except where a task's
own text says otherwise. `dira` does not exist — this lane deliberately contains only
work that needs no binary: the fixture ledger, the two recording harnesses, and the
checkers that bind them to `.agents/product-marketing.md` §6. Recording the actual
clips is `E8-L4`, which cannot start until E0–E3 ship.

**7 tasks (≤8 per the L2 bound).** Waves: `[T1]` → `[T2, T3, T4, T5]` → `[T6]` → `[T7,
external]`. T2–T5 share no file and can run concurrently once T1 lands; T6 reads T3's
harness so it waits on it; T7 is named but cannot start until E0 ships a schema
validator, and is included so the obligation is recorded rather than forgotten.

## The fixture already exists — reuse it, do not re-author it

`docs/plan/lanes/E8.md` flags `fixtures/demo-ledger/` as a shared dependency with E3,
whose lane file (`docs/plan/lanes/E3.md:123`) states "the fixture `dec-0060` reproduces
the README/marketing example exactly," and **E3-L1-T2 has already built it** —
`internal/enforcer/testdata/ledgers/daemon/dec-0060.md` and
`internal/enforcer/testdata/ledgers/daemon/int-0002.md` are on disk today, schema-valid,
and their content is byte-for-byte what `.agents/product-marketing.md` §6 quotes: title
"Track run state with a checkpoint file, not a daemon", accepted 2026-07-03, rejected
alternative `"a daemon"` with `why_not: "violates the single-binary intent (int-0002)"`
and `revisit_if: "cold-start latency stops being the binding constraint"`. Authoring a
second `dec-0060` here would be exactly the divergence-risk `docs/plan/lanes/E8.md`
warns about — one of the two fixtures would quietly be lying. So **T1 copies, and every
downstream task in this lane treats E3-L1's files as the single source of truth.**
Single owner, per the lane's own instruction: E3-L1 authors and owns the content;
this lane owns the copy that ships alongside the recording harness and the checker that
keeps the copy honest.

## Absolutes this lane is held to

1. Nothing outward-facing is published by an agent — this lane produces files only.
2. No fake output. If `dira` is absent, the harness fails loudly and emits no cast —
   never a hand-written imitation of what `dira check` would print.
3. No virality claims anywhere in the assets or their READMEs.

---

## Tasks

- [ ] E8-L3-T1 — Copy E3-L1's `dec-0060`/`int-0002` fixture into `fixtures/demo-ledger/`, byte-identical · verifies: [dec-0010, dec-0003] · deps: [E3-L1-T2 (external, already shipped)]
- [ ] E8-L3-T2 — Write the zero-dependency fixture-ledger validator, two-sided · verifies: [cst-0002, dec-0002] · deps: [E8-L3-T1]
- [ ] E8-L3-T3 — Write `assets/demo/record-check.sh`, fails loudly with no cast when `dira` is absent · verifies: [dec-0010, int-0002] · deps: [E8-L3-T1]
- [ ] E8-L3-T4 — Write `assets/demo/record-init.sh`, same absence guard for the secondary clip · verifies: [dec-0010, int-0002] · deps: [E8-L3-T1]
- [ ] E8-L3-T5 — Write `assets/demo/README.md`, the reproduction contract and the two duration/no-audio bars · verifies: [dec-0010] · deps: [E8-L3-T3, E8-L3-T4]
- [ ] E8-L3-T6 — Write `check-demo-script.mjs`, binding the harness verbatim to product-marketing §6 · verifies: [dec-0010] · deps: [E8-L3-T3]
- [ ] E8-L3-T7 — Register `fixtures/demo-ledger/` into E0's schema-validator corpus — BLOCKED on E0 · verifies: [cst-0002] · deps: [E8-L3-T2, E0]

---

### E8-L3-T1 — Copy E3-L1's `dec-0060`/`int-0002` fixture into `fixtures/demo-ledger/`, byte-identical

Two files, copied verbatim from `internal/enforcer/testdata/ledgers/daemon/`: `dec-0060`
(the decision the demo's `dira check` call conflicts with) and `int-0002` (the intent
`dec-0060`'s single `edges[].derives_from` points at, and the same id its `why_not` text
cites). Nothing else — the prompt's "small enough to read in one screen" bar is best met
by copying only what the demo output actually names, not the whole 9-file corpus E3-L1
built for its own adversarial matcher.

**Files:** `fixtures/demo-ledger/dec-0060.md`, `fixtures/demo-ledger/int-0002.md`

**acc:** both files exist; `shasum -a 256 fixtures/demo-ledger/dec-0060.md` (first field)
equals `shasum -a 256 internal/enforcer/testdata/ledgers/daemon/dec-0060.md` (first
field), and the same holds for `int-0002.md` — proving the copy, not a re-author. A
constructed adversarial copy (one character changed in the frontmatter `why_not`) is
shown to fail that same comparison, so the check is proven able to catch drift before it
is shown to pass on the real copy.

**depends_on:** `E3-L1-T2` (external, in `docs/plan/tasks/E3-L1.md`) — already shipped,
verified present on disk at the source path above, so this task is startable today
despite the cross-epic dependency. **verifies:** `dec-0010` (the fixture is what the
growth-engine demo shows), `dec-0003` (the fixture's `alternatives[].why_not` is what a
regex tier could never have produced — it is human-authored, matching the demo's claim).

---

### E8-L3-T2 — Write the zero-dependency fixture-ledger validator, two-sided

No `ajv`, per the lane's zero-dependency rule — E0 already owns real JSON-Schema
validation in Go; this checker verifies only the required fields and the closed `kind`
enum directly against `schema/entry.schema.json`'s literal values, and separately
asserts the one property the demo depends on: `dec-0060.alternatives[].why_not` is
non-empty.

**Files:** `docs/growth/scripts/check-fixture-ledger.mjs`,
`docs/growth/fixtures/demo-ledger-bad-kind/bogus-0001.md`

**acc:** `node docs/growth/scripts/check-fixture-ledger.mjs` exits 0 against
`fixtures/demo-ledger/`, reporting 0 errors and the file count checked; the same command
pointed at `docs/growth/fixtures/demo-ledger-bad-kind/` (one committed fixture whose
`kind` is `"habit"`, outside the five-value enum) exits non-zero, naming the file and the
offending `kind` value; and the same run asserts `dec-0060.md`'s `alternatives` array has
`>= 1` entries each with a non-empty `why_not`, failing named (`"dec-0060 has no
alternatives[].why_not"`) against a second constructed bad fixture with an empty
`why_not` string, and passing on the real T1 copy.

**depends_on:** `E8-L3-T1` · **verifies:** `cst-0002` (the closed five-kind enum), `dec-0002`
(one file per entry — the validator walks a directory of files, not a single blob).

---

### E8-L3-T3 — Write `assets/demo/record-check.sh`, fails loudly with no cast when `dira` is absent

Mirrors `kazi/priv/examples/hero_cast_demo/record.sh`'s contract: the committed cast is
producible only by this script, it runs the real binary against a fresh copy of
`fixtures/demo-ledger/`, and any post-processing is documented line by line. `dira` does
not exist yet, so today every run of this script must take the absence branch — that
branch is what this task actually proves.

**Files:** `assets/demo/record-check.sh`

**acc:** run with `PATH` set to an empty temp directory (no `dira` reachable), the script
exits non-zero, prints one named error identifying `dira` as absent from `PATH`, and
`assets/demo/check.cast` does not exist before or after the run. A mutated copy of the
script with its `command -v dira` guard deleted is shown, under the same empty `PATH`, to
fall through to the real recording attempt instead of failing at the guard — proving the
guard is what produces the named failure in the real script, not an unrelated crash.

**depends_on:** `E8-L3-T1` (records against the fixture it copies) · **verifies:**
`dec-0010` (no fabricated clip — a fake would misrepresent working software), `int-0002`
(the script itself has no daemon, no server — a plain script run once).

---

### E8-L3-T4 — Write `assets/demo/record-init.sh`, same absence guard for the secondary clip

Same shape as T3, for `dira init` against a repo with existing history. Which repo and
which verb resolves the import path is **not settled here** — that is `E8-L4`'s call
once E0–E3 exist and `dec-0028`'s dry-run behavior is wired — this task only builds the
guard that must hold regardless of which repo is eventually chosen.

**Files:** `assets/demo/record-init.sh`

**acc:** same two-sided proof as T3's acc line, applied to `record-init.sh`: absent-`dira`
run exits non-zero with a named error and emits no `assets/demo/init.cast`; the
guard-removed mutant falls through instead of failing at the guard, under the same empty
`PATH`.

**depends_on:** `E8-L3-T1` · **verifies:** `dec-0010`, `int-0002`.

---

### E8-L3-T5 — Write `assets/demo/README.md`, the reproduction contract and the two duration/no-audio bars

States how to reproduce both clips, which tool records them, and the numeric bars
`E8-L4`'s probes will measure — so the bar is written down once, here, rather than
invented twice.

**Files:** `assets/demo/README.md`

**acc:** `grep -c '20.0s\|20 seconds' assets/demo/README.md` is `>= 1` (the primary clip's
bound, stated once); `grep -c '60.0s\|60 seconds' assets/demo/README.md` is `>= 1` (the
secondary clip's bound); `grep -c 'zero audio streams\|no audio' assets/demo/README.md`
is `>= 1`; `grep -c 'record-check.sh' assets/demo/README.md` and `grep -c
'record-init.sh' assets/demo/README.md` are each `>= 1`; and the file states explicitly,
today, that neither cast has been recorded and why (`grep -c 'no dira binary\|does not
exist' assets/demo/README.md` is `>= 1`) — an honest README rather than one implying
clips exist that don't.

**depends_on:** `E8-L3-T3`, `E8-L3-T4` · **verifies:** `dec-0010`.

---

### E8-L3-T6 — Write `check-demo-script.mjs`, binding the harness verbatim to product-marketing §6

The one checker in this lane that keeps the clip and the positioning doc from drifting
apart. It reads the typed command and the expected output lines out of
`assets/demo/record-check.sh` (T3) — not out of a second, hand-copied literal — and
asserts each appears verbatim in `.agents/product-marketing.md` §6.

**Files:** `docs/growth/scripts/check-demo-script.mjs`

**acc:** `node docs/growth/scripts/check-demo-script.mjs` exits 0 against the real
`assets/demo/record-check.sh` and `.agents/product-marketing.md`, naming the file and
line range it checked; a constructed copy of `.agents/product-marketing.md` with a
single character changed inside the §6 fenced block (e.g. `why_not` → `why not`) flips
the same command to a named non-zero failure quoting the mismatched line — proven before
the untouched pair is shown to pass.

**depends_on:** `E8-L3-T3` · **verifies:** `dec-0010`.

---

### E8-L3-T7 — Register `fixtures/demo-ledger/` into E0's schema-validator corpus — BLOCKED on E0

The zero-dependency rule above exists only because no Go schema validator exists yet.
Once E0 ships one, `fixtures/demo-ledger/` should be added to whatever corpus that
validator runs against, so a future schema change is caught in Go rather than only in
this lane's hand-rolled node checker. **This task cannot be given a concrete `acc:` line
today** because it names a Go package and test function that do not exist — recorded
honestly per the lane's honesty rule rather than invented against an interface nobody
has built.

**Files:** TBD — whichever package E0 lands its schema validator in (see
`docs/plan.md`'s note that E0-L4/E0-L5 need re-reading against the current tree before
decomposition).

**acc (to be sharpened once E0 lands):** the Go package that validates
`schema/entry.schema.json` includes `fixtures/demo-ledger/` in its test corpus, and its
test suite fails if either fixture file is removed.

**depends_on:** `E8-L3-T2`, and **E0** (external epic — not yet decomposed to executable
fidelity; see `docs/plan.md` §"E0 tail"). **verifies:** `cst-0002`.

---

## Not decomposed further

No task above needs an L3 split. T1 is a two-file copy with a checksum assertion; T2–T4
are each one script (or one script plus a fixture) with a two-sided test; T5 is prose
with grep-checkable bars; T6 is one verbatim-binding checker; T7 is intentionally left
under-specified because the interface it targets does not exist, per the honesty rule —
sharpening it further would be inventing a spec against nothing.

## What this lane does not own

The clips themselves (`assets/demo/check.cast`, `assets/demo/init.cast`) — that is
`E8-L4`, and it cannot start until `dira check` and `dira init` actually run. The
duration probe, the no-audio probe, and the drift-vs-recording check are `E8-L4`'s to
build, reading this lane's `check-demo-script.mjs` and `record-*.sh` rather than
reimplementing their expected-output logic. Which repo `dira init` records against for
the secondary clip, and whether `dira init` or a separate `dira import` verb does the
work (`dec-0028` names `dira import <dir>` as the measuring verb) — both are `E8-L4`
decisions, made once the binary exists to decide against.

## Report

Task count: 7. Path: `docs/plan/tasks/E8-L3.md`. **`dec-0060` was already
built** by `E3-L1-T2` and is schema-valid and byte-identical to what
`.agents/product-marketing.md` §6 quotes — T1 reuses it rather than re-authoring it, and
the shared-fixture risk `docs/plan/lanes/E8.md` names is resolved by giving E3-L1 sole
authorship and this lane a checksum-verified copy. The fixture and its owner are correct
in `.agents/product-marketing.md` §6 as written — no correction needed there. T7 is
refused a concrete `acc:` line and named `BLOCKED on E0` rather than invented, per the
honesty rule; every other task is buildable today with no binary.
