---
id: dec-0003
kind: decision
title: "No model client in the binary"
state: accepted
created: "2026-07-29T09:00:00Z"
edges:
  - type: derives_from
    to: "int-0002"
alternatives:
  - option: "Embed an API client and call a model directly"
    why_not: "it puts a key, a bill and a network dependency inside a tool whose whole claim is that it needs none of them."
---

The already-running agent session does the semantic work. dira never embeds a model client, so there is no api tier.
