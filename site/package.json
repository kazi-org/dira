{
  "name": "dira-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "gen": "node scripts/snapshot-why.mjs && node scripts/gen-verb-tour.mjs",
    "build": "npm run gen && astro build",
    "preview": "astro preview",
    "check:coherence": "node scripts/check-coherence.mjs",
    "test": "playwright test"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@tailwindcss/vite": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
